# Results

效能測試結果存檔。

| 檔案 | 說明 |
|------|------|
| `perf-latest.json` | 最新測試數據 |
| `perf-baseline-*.md` | 基準線記錄 |
| `perf-comparison-*.md` | 比較報告 |
| `bench-*.json` | 歷史測試數據 |

由 `bench.py` 自動產出，不需要手動維護。
