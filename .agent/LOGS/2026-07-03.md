# 2026-07-03 (Session 2)

## 目標

找出蝦皮簽到 API 的正確 endpoint 與參數，更新偵錯腳本。

## 完成項目

- [x] 撰寫獨立 debug 腳本 `蝦皮Debug-攔截簽到請求.js`，hook XHR/fetch 攔截簽到請求
- [x] 主腳本 `蝦皮首頁觸發自動簽到.js` 改寫為 v4.0：不在 `/shopee-coins` 頁面時用隱藏 iframe 載入並點擊按鈕
- [x] 修正網域過濾邏輯（只攔截 `*.shopee.tw`，排除 Google Analytics 等外部服務）
- [x] 使用者點擊簽到後成功攔截到實際 API 請求
- [x] 確認正確 endpoint：`https://games-dailycheckin.shopee.tw/mkt/coins/api/v2/checkin_new`
- [x] 確認必要 headers：`dci-version: 4008000`、`check-in-origin: pc`、`Content-Type: application/json`
- [x] 確認 request body 需動態 token：`dfp` 與 `s`（一次性簽章，來源待查）
- [x] Debug 腳本升級 v1.1，加入 Response 攔截（HTTP 狀態碼 + 回傳內容）

## 已知問題

- `dfp` 與 `s` token 為一次性動態值，無法直接重現，需從頁面 JS 找出生成邏輯或改用 iframe/點擊方案
- 今日簽到已完成（頁面顯示「明天再回來」），需等下次測試

## 待辦（移至下個 session）

- [ ] 使用者於可簽到時測試 v4.0（無論從首頁或 `/shopee-coins`），確認自動簽到成功

## 備註

- Shopee coins SPA 使用 lazy-loaded webpack chunks，coin 相關程式碼不在主 bundle 中
- 攔截到的主機為 `games-dailycheckin.shopee.tw`，不同於舊有資料的 `shopee.tw`
